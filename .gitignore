# Python 仮想環境（巨大・各自で再生成）
venv/
.venv/
__pycache__/
*.pyc

# 自動ダウンロードされるモデル（初回実行時に取得される）
models/

# Claude Code などのローカル設定
.claude/

# LLM(Bedrock)設定（AWS認証情報を含むため公開しない）
config/

# 音声・出力ファイル（個人情報を含む可能性があるため公開しない）
*.wav
*.srt
INPUT/
OUTPUT/
_*.txt
server.log
